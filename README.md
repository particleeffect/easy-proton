## easy proton

### useage
```javascript
import { createProton, destroyProton } from "easy-proton";

createProton({
    rate = 30,
    body,
    life = 1,
    x = 100,
    y = 100,
    canvas,
    color = ["#ff0000", "#ffff00"],
    scale = [1, 0.1],
    alpha = [1, 0],
    v = 2,
    angel = [0, 50]
});

if btn.onclick
destroyProton();

```
